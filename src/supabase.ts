/**
 * BarakAPP v0.2.x funciona exclusivamente en modo local con Electron.
 *
 * Este archivo se mantiene temporalmente para neutralizar una versión antigua
 * que podía seguir presente en el repositorio y que importaba
 * @supabase/supabase-js. La integración con Supabase se reintroducirá cuando
 * pasemos de la fase local a la fase servidor.
 */
export const supabaseEnabled = false
export const supabase = null
